type IntSlice = Slice<int>;

fn main() -> int {
    let data: int[3] = { 1, 2, 3 };
    let values: IntSlice;
    values.data = &data[0];
    values.length = 3;

    let total: int = 0;
    foreach index, item in values {
        total = total + index + item;
    }

    return total - 9;
}
